you need a virtual name based Host withlocal moodle instance (maybe https is needed)

To connect the moodle from the virtual device, you can have to edit the hosts file in the virtual device every time after you started it.

After changing the hostname you can use the edit_host_in_android_vh.sh script:

 
